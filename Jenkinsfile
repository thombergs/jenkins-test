pipeline {
     agent any
     stages {
         stage('Build') {
             steps {
                 echo 'Building...'
             }
         }
         stage('deploy to prod') {
             steps {
                 echo 'Deploying...'
             }
         }
     }
 }
