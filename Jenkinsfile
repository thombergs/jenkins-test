pipeline {
     
     agent any
     
     stages {
         
         stage('build') {
             steps {
                 echo 'Building...'
             }
         }

         stage('deploy to test') {
             steps {
                 echo 'Deploying...'
             }
         }
        
         stage('deploy to staging') {
             steps {
                 echo 'Deploying...'
             }
         }
         
         stage('deploy to prod') {
             steps {
                 echo 'Deploying...'
             }
         }
     }
 }
